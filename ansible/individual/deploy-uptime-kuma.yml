- name: Deploy uptime-kuma
  hosts: homelab
  become: true

  vars:
    uptime_kuma_path: /home/pratikserver/docker/uptime-kuma

  tasks:
    - name: Check directory exists
      stat:
        path: "{{ uptime_kuma_path }}"
      register: directory_register_record

    - name: Fail if directory missing
      fail:
        msg: "Uptime Kuma directory not found"
      when: not directory_register_record.stat.exists

    - name: Check docker-compose.yml exists
      stat:
        path: "{{ uptime_kuma_path }}/docker-compose.yml"
      register: compose_register_record

    - name: Fail if docker-compose.yml missing
      fail:
        msg: "Uptime Kuma docker-compose.yml missing"
      when: not compose_register_record.stat.exists

    - name: Pull latest image
      command: docker compose pull
      args:
        chdir: "{{ uptime_kuma_path }}"

    - name: Start/update container
      command: docker compose up -d
      args:
        chdir: "{{ uptime_kuma_path }}"
