services:
  tunnel:
    container_name: cloudflared-tunnel
    image: cloudflared/cloudflared:1826-d2a87e9b9345
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${TUNNEL_TOKEN}
    networks:
      - docker-cloudflared

networks:
  docker-cloudflared:
    external: true
