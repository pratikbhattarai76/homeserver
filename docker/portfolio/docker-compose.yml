services:
  portfolio-app:
    image: ghcr.io/pratikbhattarai76/portfolio-app:latest
    container_name: portfolio-app
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - docker-proxy

networks:
  docker-proxy:
    external: true
